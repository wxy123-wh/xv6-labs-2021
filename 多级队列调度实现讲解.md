# xv6 多级队列（MLQ / MLFQ）调度实现（小白友好版）

> 本文解释你当前 xv6 代码里“多级队列调度”是怎么实现的。  
> 前一篇文档讲了“优先级调度”，这里讲的是 `sched_policy == SCHED_MLQ` 时的另一套逻辑。

---

## 1. 多级队列调度是个啥？为什么要搞这个？

现实世界里，有的程序是：

- 一直算一直算（视频编码、压缩）——**CPU 密集型**
- 经常等 I/O（等键盘、网络、磁盘）——**I/O 密集型**

我们希望：

1. **响应速度好**：I/O 密集型进程被唤醒时，能很快抢到 CPU。
2. **避免被某个进程长期霸占 CPU**：让 CPU 密集型进程适当地“下放”，别一直在最高优先级。

多级队列（MLQ / MLFQ）的经典做法：

- 把就绪队列分成多层（比如高、中、低三层）。
- 调度器每次总是先看高层队列，再看中层，再看低层。
- 高层队列的时间片短，低层队列的时间片长。
- 新来的或刚被 I/O 唤醒的进程通常放到高层。
- 用完整个时间片的进程会被“降级”到更低层。

你现在的实现基本就是这个套路。

---

## 2. 相关参数和辅助函数在哪儿？

看 `kernel/param.h` 中关于 MLQ 的定义：

```c
// 多级队列参数：3 层（高/中/低）
#define MLQ_LEVELS        3
#define MLQ_HIGH          0
#define MLQ_MEDIUM        1
#define MLQ_LOW           2

// 每个队列的时间片长度
#define QSLICE_HIGH   5
#define QSLICE_MEDIUM 10
#define QSLICE_LOW    20
```

再看 `kernel/proc.h` 的辅助函数：

```c
static inline int prio_to_mlq_level(int prio){
  if (prio <= 5)  return MLQ_HIGH;
  if (prio <= 10) return MLQ_MEDIUM;
  return MLQ_LOW;
}

static inline int qslice_for_level(int lvl){
  switch (lvl) {
    case MLQ_HIGH:   return QSLICE_HIGH;
    case MLQ_MEDIUM: return QSLICE_MEDIUM;
    default:         return QSLICE_LOW;
  }
}
```

解释：

- `prio_to_mlq_level()`：用你已有的 `priority` 字段当作“初始层级”的参考：
  - 优先级数值越小（越重要），就会被映射到更高层。
- `qslice_for_level()`：每层队列对应不同时间片：
  - 高层队列时间片短 → 切换频繁，响应好；
  - 低层队列时间片长 → 对长任务友好，但调度优先级低。

---

## 3. 多级队列的数据结构长什么样？

### 3.1 全局队列：`struct multilevel_queue`

在 `kernel/proc.h`：

```c
struct multilevel_queue {
  struct proc *head[MLQ_LEVELS];
  struct proc *tail[MLQ_LEVELS];
};

extern struct multilevel_queue mlq;
```

含义：

- 有 3 个队列（高、中、低），每层都有一个 `head` 和 `tail` 指针。
- 队列内部用双向链表串起各个进程。

### 3.2 进程里的队列字段

在 `struct proc`（`kernel/proc.h`）里：

```c
struct proc {
  ...
  struct proc *rq_next;
  struct proc *rq_prev;
  int qlevel;            // 当前在哪一层队列（0/1/2 或 -1）
  int cur_qslice_left;   // 当前层级剩余时间片
  ...
};
```

- `rq_next` / `rq_prev`：链表指针，用来把进程串成队列。
- `qlevel`：
  - `0`、`1`、`2`：分别代表高、中、低三层；
  - `-1`：不在任何队列中（比如暂时 RUNNING，或在另一种策略）。

### 3.3 队列操作函数：`mlq_*`

在 `kernel/proc.c` 顶部可以看到：

```c
struct multilevel_queue mlq;

// 初始化 MLQ
static void mlq_init(void){
  for (int i = 0; i < MLQ_LEVELS; i++) {
    mlq.head[i] = 0;
    mlq.tail[i] = 0;
  }
}
```

#### 3.3.1 入队：`mlq_enqueue_level(struct proc *p, int lvl)`

逻辑（简化说明）：

1. 找到对应层级的 `head` 和 `tail` 指针地址。
2. 把 `p` 插到队尾（尾插法）：
   - `p->rq_next = 0;`
   - `p->rq_prev = *tail;`
3. 如果原来队列不空：
   - `(*tail)->rq_next = p;`
   - 否则说明队列空，`*head = p;`
4. 最后更新 `*tail = p;`，并设置 `p->qlevel = lvl;`

> 这样，每一层队列内部都是“先来先服务”的顺序。

#### 3.3.2 出队/移除：`mlq_remove_level(struct proc *p)`

逻辑：

1. 取出 `p->qlevel`，如果不在合法范围（0~2），直接返回。
2. 根据层级拿到 `head` / `tail`。
3. 调整前后指针：
   - 如果 `p` 有前驱：`p->rq_prev->rq_next = p->rq_next`；
   - 否则说明 `p` 是队头：`*head = p->rq_next`。
   - 同理处理后继更新 `*tail`。
4. 最后把 `rq_next`、`rq_prev` 置 0，把 `qlevel` 设为 -1。

#### 3.3.3 选队头：`mlq_pick_head()`

逻辑很清晰：

1. 从 `MLQ_HIGH` 往 `MLQ_LOW` 循环：
   - 对于每一层，拿 `*head`。
   - 如果非空，直接返回这个队头进程。
2. 如果三层都空，返回 `0`。

> 这就实现了“高层优先”的调度策略。

---

## 4. 包一层通用接口：`rq_enqueue` / `rq_remove` / `rq_pick_next`

为了让“调度器”和“其它代码”不直接依赖 MLQ 细节，代码封装了几层通用接口。

### 4.1 `rq_enqueue(struct proc *p)`

伪代码：

```c
if (sched_policy == SCHED_MLQ) {
  // 决定该进程要进哪一层
  int lvl = (p->qlevel 在合法范围) ? p->qlevel : prio_to_mlq_level(p->priority);
  mlq_enqueue_level(p, lvl);
  p->cur_qslice_left = qslice_for_level(lvl);
} else {
  // 优先级调度模式：不使用 MLQ
  if (p->qlevel >= 0) mlq_remove_level(p); // 防止残留
  p->qlevel = -1;
  p->rq_next = p->rq_prev = 0;
  p->cur_qslice_left = FIXED_QSLICE_PRIORITY;
}
```

在 MLQ 模式下：

- 如果已经有 `qlevel`，就继续用这个层级；
- 否则根据 `priority` 计算初始层级；
- 根据层级设置不同时间片。

### 4.2 `rq_remove(struct proc *p)`

伪代码：

```c
if (sched_policy == SCHED_MLQ) {
  if (p->qlevel >= 0) mlq_remove_level(p);
} else {
  p->qlevel = -1;
  p->rq_next = p->rq_prev = 0;
}
```

主要用于：

- 进程从队列取出要运行；
- 进程 `sleep` 时从队列中移除，防止“既在队列又在睡觉”。

### 4.3 `rq_pick_next()`

伪代码：

```c
if (sched_policy == SCHED_MLQ) {
  return mlq_pick_head();      // 多级队列：高→低，按队头选
} else {
  return priority_pick_best(); // 优先级调度：直接遍历 NPROC
}
```

这样，调度器只要调用 `rq_pick_next()`，不用管下面到底是“多级队列”还是“扫描所有进程”。

---

## 5. 调度器 `scheduler()` 在 MLQ 模式下的工作流程

打开 `kernel/proc.c` 中的 `scheduler()` 函数，关注 `sched_policy == SCHED_MLQ` 分支。

### 5.1 启动时的队列初始化

调度器一开始会做：

```c
if (sched_policy == SCHED_MLQ) {
  mlq_init();  // 清空三层队列
  for (p = proc; p < &proc[NPROC]; p++) {
    acquire(&p->lock);
    if (p->state == RUNNABLE) {
      p->qlevel = prio_to_mlq_level(p->priority);
      rq_enqueue(p);
    }
    release(&p->lock);
  }
}
```

说明：

- 系统刚启动的时候，可能已经有一些 RUNNABLE 进程（比如 `init`）。
- 我们把它们按照 `priority` 映射到对应的队列层级。

### 5.2 主循环里的 MLQ 分支

核心逻辑（简化）：

```c
for (;;) {
  intr_on();  // 开中断

  struct proc *best_proc = 0;

  if (sched_policy == SCHED_MLQ) {
    best_proc = rq_pick_next();  // 实际调用 mlq_pick_head()
    if (best_proc) {
      acquire(&best_proc->lock);
      if (best_proc->state == RUNNABLE) {
        rq_remove(best_proc);    // 从对应层级队列中移除
        best_proc->state = RUNNING;
        c->proc = best_proc;
        swtch(&c->context, &best_proc->context);
        c->proc = 0;
        best_proc->wait_time = 0;
      }
      release(&best_proc->lock);
    }
  } else {
    ... // 优先级调度分支
  }
}
```

也就是说：

- 每一轮调度：
  - 优先看高层队列队头；
  - 高层空了再看中层；
  - 都空了再看低层；
- 找到队头进程后：
  - 先从队列中移除；
  - 切到该进程运行；
  - 等它运行完一个时间片或主动让出后，再回来下一轮。

---

## 6. 时间片耗尽时如何“降级”队列？

关键仍然在 `on_tick_running(struct proc *p)`，只是这次看的是 `SCHED_MLQ` 分支。

```c
if (sched_policy == SCHED_MLQ) {
  int lvl = (p->qlevel >= 0) ? p->qlevel : MLQ_LOW;
  int newlvl = (lvl < MLQ_LOW) ? (lvl + 1) : MLQ_LOW;

  acquire(&p->lock);
  p->state = RUNNABLE;

  if (p->qlevel >= 0) {
    mlq_remove_level(p);      // 从旧层队列中删掉
  }
  mlq_enqueue_level(p, newlvl); // 插入新层队列尾部

  p->qlevel = newlvl;
  p->cur_qslice_left = qslice_for_level(newlvl);
  release(&p->lock);

  return 1; // 告诉 trap：“时间片用完，可以让出”
}
```

解释：

- 每次时钟中断都在 `on_tick_running()` 里减 `cur_qslice_left`。
- 当减到 ≤0 时：
  1. 认为时间片耗尽。
  2. 把进程从原来的层队列中移除。
  3. 层级 +1（向下移动），最多到 `MLQ_LOW`。
  4. 把进程插到新层队列的尾部。
  5. 给它分配新层级对应的时间片。
  6. 返回 1，让 `usertrap()` 调用 `yield()`。

结果就是：

- 一直大量用 CPU 的进程，会慢慢从高层掉到中层，再掉到低层。
- 高层队列主要给“短任务 / 刚唤醒的 I/O 任务”使用。

---

## 7. 主动让出 CPU 时也会降级：`yield()` 中的 MLQ 逻辑

`yield()` 是进程主动谦让 CPU 的系统调用。  
在 MLQ 策略下，代码里还增加了一点“小惩罚”：

```c
void yield(void) {
  struct proc *p = myproc();
  acquire(&p->lock);

  if (sched_policy == SCHED_MLQ) {
    int old = p->qlevel >= 0 ? p->qlevel : prio_to_mlq_level(p->priority);
    int newlvl = old < MLQ_LOW ? (old + 1) : MLQ_LOW;
    p->qlevel = newlvl;
  }

  p->state = RUNNABLE;
  rq_enqueue(p);
  sched();
  release(&p->lock);
}
```

含义：

- 在 MLQ 模式下：
  - 进程主动 `yield()` 时，会被“降一级队列”（如果还不是最低层）。
  - 然后再调用 `rq_enqueue(p)`，按新的 `qlevel` 放入对应队列。

直觉上：

- 越“老实”在 CPU 上跑满时间片或频繁 yield 的 CPU 密集任务，会慢慢被移到低优先级队列。
- I/O 型或短任务，因为经常 sleep/wakeup，而不是长时间占用 CPU，会一直被提升到高层（下一节会讲）。

---

## 8. I/O 睡眠 & 唤醒：如何“提升”队列层级？

### 8.1 `sleep()`：挂起自己等待某个事件

在 `kernel/proc.c` 的 `sleep(chan, lk)` 中：

```c
if (sched_policy == SCHED_MLQ) {
  if (p->state == RUNNABLE) {
    rq_remove(p); // 防止“既在队列里，又在睡眠中”
  }
}

p->chan = chan;
p->state = SLEEPING;

sched();
```

大意：

- 进程在 MLQ 模式下如果本来就在就绪队列里，就先把它从队列中移除。
- 然后改成 `SLEEPING` 状态，进入 `sched()`，让出 CPU。

### 8.2 `wakeup()` + `on_wakeup()`：把 I/O 完成的进程推上高层

在 `wakeup(chan)` 中：

1. 遍历所有进程 `p`：
   - 找到 `p->state == SLEEPING && p->chan == chan` 的。
2. 对这样的进程：
   - 先释放 `p->lock`；
   - 调用 `on_wakeup(p)` 来完成唤醒逻辑；
   - `on_wakeup(p)` 会返回一个 `suggest_preempt`，表示“建议抢占吗？”。

重点看 `on_wakeup(p)` 在 MLQ 模式下的分支：

```c
if (sched_policy == SCHED_MLQ) {
  if (p->qlevel >= 0) {
    mlq_remove_level(p);
  }

  int newlvl = MLQ_HIGH;           // 直接提升到最高层
  mlq_enqueue_level(p, newlvl);
  p->qlevel = newlvl;
  p->cur_qslice_left = qslice_for_level(newlvl);
  release(&p->lock);

  return 1; // 建议抢占当前运行的进程
}
```

解释：

- 每次 I/O 完成后，这个进程被唤醒：
  - 它会被放到 **最高层队列**；
  - 得到高层对应的短时间片（响应更快）；
  - `on_wakeup()` 返回 `1`，表示“建议抢占”。

然后 `wakeup()` 会根据这个建议决定是否 `yield()` 当前进程：

```c
if (suggest_preempt && current && current->state == RUNNING) {
  if (sched_policy == SCHED_MLQ) {
    yield();
    return;
  }
}
```

这就实现了经典 MLFQ 的一个重要特性：

> **I/O 型进程一被唤醒，就能比较快抢到 CPU 来继续处理，从而提高系统的交互性。**

---

## 9. 用户态测试程序 `mytest3.c` 是怎么验证 MLQ 的？

看 `user/mytest3.c`，里面创建了三个不同风格的进程：

1. `CPU-A(high->low)`：
   - `cpu_task()`：大量 CPU 计算，偶尔 `sleep(1)` 或打印。
   - 预期：一开始在高层，随着多次用完时间片/主动让出，会被逐步降到中层、低层。
2. `IO-B(boosted)`：
   - `io_task()`：频繁 `sleep(sleep_ticks)`，然后被 `wakeup()` 叫醒。
   - 预期：每次唤醒都被 `on_wakeup()` 提升到高层，并抢占 CPU。
3. `MID-C(mixed)`：
   - `medium_task()`：中等强度 CPU + 偶尔 sleep / yield。
   - 预期：可能在中层和高层之间来回，表现“中度任务”的调度特性。

通过观察：

- 各进程打印的时间（ticks）、顺序、是否经常被插队；
- 你可以直观感受到：
  - I/O 进程确实“醒来就抢 CPU”；
  - CPU 进程慢慢被“下放”到低层；
  - 高层队列总是被优先调度。

---

## 10. 把这一切串起来：作为小白你要记住哪几个点？

1. **多级队列结构**：
   - 有 3 层队列（高/中/低），每层一个 FIFO。
   - 每个进程有 `qlevel`、`rq_next`、`rq_prev`。
2. **调度器如何选进程**：
   - 总是先看高层队列队头；
   - 高层空了再看中层，再看低层；
   - 选到进程后从队列里移除，切过去跑。
3. **时间片用完会降级**：
   - 时钟中断调用 `on_tick_running()`；
   - 时间片减到 0 → 把进程从旧层队列移除，降级到下一层队列尾部；
   - 重新分配更长/更低优先级的时间片。
4. **I/O 唤醒会升级 + 抢占**：
   - `sleep()` 时从队列移除、标记为 SLEEPING；
   - `wakeup()` 找到后调用 `on_wakeup()`：
     - 直接放到最高层队列；
     - 建议抢占当前运行进程（调用 `yield()`）。
5. **主动 yield 也会让队列层级↓**：
   - 在 MLQ 策略下，`yield()` 会把 `qlevel` +1（降一层），然后重新入队。

只要把这 5 点搞清楚，再对照源码看几遍，你就能逐步看懂并修改自己的调度器，让它做更多“花活”（比如增加更多层、增加不同的时间片、加入统计信息等）。

如果配合上一份《优先级调度算法实现讲解》，你就同时掌握了两种典型的内核调度策略。

