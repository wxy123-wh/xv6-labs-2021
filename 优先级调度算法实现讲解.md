# xv6 优先级调度算法实现（小白友好版）

> 本文结合你当前仓库里的代码（主要是 `kernel/proc.c`、`kernel/proc.h`、`kernel/param.h`、`kernel/sysproc.c` 等），用尽量通俗的方式解释“优先级调度”是怎么实现的。  
> 如果你几乎没写过内核代码，也可以当成“带着你看源码”的说明书。

---

## 1. 先说说我们要实现什么

在操作系统里，**调度器**就是“发话让谁上 CPU 干活的人”。

你现在的 xv6 被改造成支持两种调度模式：

- `SCHED_PRIORITY`：**优先级调度**
- `SCHED_MLQ`：多级队列调度（另外一篇文档讲）

**本篇只聚焦优先级调度模式**，也就是 `sched_policy == SCHED_PRIORITY` 的时候：

1. 每个进程有一个整数 `priority`（0 ~ 31）：
   - 数值越小，优先级越高（0 最高，31 最低）。
2. 调度器每次从所有 `RUNNABLE`（就绪）进程中选一个：
   - 谁的 `priority` 最小，就让谁上 CPU。
3. 有“老化”机制，避免低优先级进程永远得不到 CPU：
   - 等得太久的进程会慢慢“加分”（优先级数字减小）。
4. 使用固定长度的时间片（比如 10 个 tick）：
   - 时间片用完，就强制让出 CPU，重新挑人。

下面我们沿着“数据结构 → 系统调用 → 核心调度器 → 中断处理”这条线看。

---

## 2. 关键数据结构：进程里都加了什么字段？

打开 `kernel/proc.h`，找到：

```c
struct proc {
  ...
  int priority;      // 进程优先级：0~31，越小越“重要”
  int wait_time;     // 在 RUNNABLE 状态下等待了多少 tick

  struct proc *rq_next;
  struct proc *rq_prev;
  int qlevel;
  int cur_qslice_left; // 当前还剩多少“时间片”
  ...
};
```

这里和优先级调度密切相关的是：

- `priority`：静态优先级。
- `wait_time`：为了实现“老化”，记录这个进程在就绪状态下等了多久。
- `cur_qslice_left`：当前这次上 CPU 还剩下多少 tick（时钟中断次数）。

> “静态优先级”可以理解为：操作系统给的基础打分，用户还能通过系统调用修改。

### 2.1 新进程的默认优先级

在 `kernel/proc.c` 的 `allocproc()` 里，找到：

```c
found:
  p->pid = allocpid();
  p->state = USED;
  p->priority = 15;
  p->wait_time = 0;
```

说明：

- 每个新创建的进程，默认优先级是 `15`（中间值）。
- 你可以用 `setpriority()` 系统调用把它调高或调低。

---

## 3. 参数和“当前调度策略”：在哪儿配？

看 `kernel/param.h`：

```c
// 调度模式定义
#define SCHED_PRIORITY    0  // 优先级调度模式
#define SCHED_MLQ         1  // 多级队列调度模式

#ifndef DEFAULT_SCHED_POLICY
#define DEFAULT_SCHED_POLICY SCHED_PRIORITY
#endif
extern int sched_policy;     // 当前调度策略

// 优先级调度的统一时间片
#define FIXED_QSLICE_PRIORITY 10
```

- `sched_policy` 是一个全局变量，在 `kernel/proc.c` 顶部有初始化：

  ```c
  int sched_policy = DEFAULT_SCHED_POLICY;
  ```

  默认就是 `SCHED_PRIORITY`，也就是优先级调度。

- `FIXED_QSLICE_PRIORITY` 表示：
  - 当我们处于“优先级调度模式”时，所有进程的时间片长度都是固定的，比如 10 个 tick。

---

## 4. 用户接口：`setpriority()` 系统调用是怎么来的？

从“用户能调用的函数”往内核里走一遍，你就明白 syscall 是怎么接进内核的。

### 4.1 用户空间：`user/user.h`

```c
int setpriority(int pid, int priority);
```

这就是你在用户程序里可以直接调用的函数原型，比如：

```c
setpriority(getpid(), 5);
```

### 4.2 系统调用号：`kernel/syscall.h`

```c
#define SYS_setpriority 25
```

每个系统调用都有一个整数编号，内核通过这个号来分发到对应的处理函数。

### 4.3 内核分发表：`kernel/syscall.c`

1. 先有外部声明：

   ```c
   extern uint64 sys_setpriority(void);
   ```

2. 然后在 `syscalls[]` 这个数组里登记：

   ```c
   static uint64 (*syscalls[])(void) = {
     ...
     [SYS_setpriority] sys_setpriority,
   };
   ```

3. 当用户执行 ecall，内核在 `syscall()` 函数里会：
   - 读寄存器 `a7` 拿到系统调用号；
   - 按这个号在 `syscalls[]` 里找到 `sys_setpriority`；
   - 执行它。

### 4.4 内核真正干活的函数：`sys_setpriority()`（在 `kernel/sysproc.c`）

核心逻辑（用白话概括）：

1. 使用 `argint(0, &pid)`、`argint(1, &priority)` 取出用户传入的参数。
2. 检查优先级范围：
   - 如果 `priority < 0 || priority > 31` 就直接返回 `-1`。
3. 遍历整个进程表 `proc[]`：
   - 对每个进程 `p`：
     - `acquire(&p->lock);`
     - 如果 `p->pid == pid`：
       - `p->priority = priority;`
       - 标记 `found = 1`，释放锁并跳出。
     - 否则释放锁，继续下一项。
4. 返回值：
   - 如果找到了进程，返回 `0`；
   - 没找到则返回 `-1`。

你可以跑 `user/finaltest.c` 和 `user/mytest2.c` 里的测试，里面大量使用了 `setpriority()` 来测试优先级调度是否生效。

---

## 5. 调度器主体：`scheduler()` 怎样按优先级选进程？

调度器的主循环在 `kernel/proc.c` 的 `scheduler()` 函数里。  
这个函数会一直循环，做三件事：

1. 打开中断（允许时钟中断等发生）。
2. 从所有进程中选一个合适的 RUNNABLE 进程。
3. 切换到该进程运行一段时间，直到它返回调度器。

我们只看 `sched_policy == SCHED_PRIORITY` 的分支。

### 5.1 简化后的伪代码思路

伪代码大致长这样（对应你代码里的 else 分支）：

```c
for (;;) {
  intr_on();          // 允许中断
  best_proc = 0;
  best_priority = 32; // 大于最大合法值 31

  // 1. 遍历所有进程，挑一个“分数最高”的
  for (p in proc[]) {
    if (p->state == RUNNABLE) {
      p->wait_time++;                 // 等待时间+1
      if (p->wait_time > 100 && p->priority > 0) {
        p->priority--;                // 老化：提高优先级
        p->wait_time = 0;
      }
      if (!best_proc || p->priority < best_priority) {
        best_proc = p;                // 记录当前最优
        best_priority = p->priority;
      }
    }
  }

  // 2. 真的有 RUNNABLE 进程，就切过去跑一会儿
  if (best_proc) {
    切换到 best_proc → RUNNING
    运行一段时间（一个时间片）后回到 scheduler
    best_proc->wait_time = 0;
  }
}
```

### 5.2 代码细节拆解

在 `scheduler()` 函数的 else 分支中：

1. 定义选择变量：

   ```c
   struct proc *best_proc = 0;
   int best_priority = 32;
   ```

2. 遍历 `proc[]`：

   ```c
   for (p = proc; p < &proc[NPROC]; p++) {
     acquire(&p->lock);
     if (p->state == RUNNABLE) {
       p->wait_time++;

       if (p->wait_time > 100 && p->priority > 0) {
         p->priority--;      // 优先级数字减小 → 优先级提高
         p->wait_time = 0;
       }

       if (!best_proc || p->priority < best_priority) {
         best_proc = p;
         best_priority = p->priority;
       }
     }
     release(&p->lock);
   }
   ```

   说明：

   - `wait_time` 是“等了多久”的计数器，只在 `RUNNABLE` 状态下累加。
   - 超过 100 就“老化”一次：优先级数字 -1（只要还大于 0）。
   - `best_proc` 和 `best_priority` 记录当前看到的最“重要”的进程。

3. 选中进程后，切过去运行：

   ```c
   if (best_proc) {
     acquire(&best_proc->lock);
     if (best_proc->state == RUNNABLE) {
       best_proc->state = RUNNING;
       c->proc = best_proc;
       swtch(&c->context, &best_proc->context);
       c->proc = 0;
       best_proc->wait_time = 0;
     }
     release(&best_proc->lock);
   }
   ```

   - `swtch()` 会保存当前 CPU 的上下文，切到进程上下文去运行。
   - 当这个进程因为时间片用完、sleep、yield 等原因又切回来时：
     - 重新回到这个 `scheduler()` 函数，继续下一轮循环。

> 直观理解：  
> 每次调度就像“全班同学做一次排名”（遍历所有 RUNNABLE 进程），分数高的（priority 值小的）先上台表演；  
> 等太久的同学会加分，避免永远没有机会。

---

## 6. 时间片+时钟中断：是谁让进程“自动让出”的？

即使一个高优先级进程什么都不干、一直死循环，也不能永远占着 CPU。  
这一点是靠**时钟中断 + 时间片**来保证的。

### 6.1 时钟中断入口：`usertrap()`（`kernel/trap.c`）

当 CPU 在用户态运行进程时，定时器会周期性发出中断。  
中断来到 `usertrap()`，里边有这样的逻辑（中文解释版）：

```c
else if ((which_dev = devintr()) != 0) {
  if (which_dev == 2) { // TIMER 中断
    if (p && p->state == RUNNING) {
      int need_yield = on_tick_running(p);
      if (need_yield) {
        yield();          // 时间片用完，主动让出 CPU
      }
    }
  }
}
```

也就是说：

- 每来一次定时器中断，就调用 `on_tick_running(p)` 看看当前进程的时间片是不是已经用完。
- 如果 `on_tick_running()` 返回 1，就调用 `yield()`：
  - 把自己标记为 `RUNNABLE`，然后回到 `scheduler()` 去重新排队。

### 6.2 `on_tick_running()` 里做了什么？

在 `kernel/proc.c` 中：

```c
int on_tick_running(struct proc *p) {
  if (!p) return 0;
  if (p->state != RUNNING) return 0;

  if (p->cur_qslice_left > 0) {
    p->cur_qslice_left--;
  }

  if (p->cur_qslice_left > 0) {
    return 0;  // 时间片还没耗尽
  }

  // 时间片耗尽
  if (sched_policy == SCHED_MLQ) {
    ... // 多级队列逻辑，另一篇讲
    return 1;
  } else {
    // PRIORITY 策略：不涉及队列层级
    acquire(&p->lock);
    p->state = RUNNABLE;
    p->cur_qslice_left = FIXED_QSLICE_PRIORITY; // 重置固定时间片
    release(&p->lock);
    return 1; // 建议让出
  }
}
```

说明：

- 每次时钟中断，`cur_qslice_left--`。
- 当减到 0 及以下时，就认为“时间片用完了”：
  - 把进程状态改回 `RUNNABLE`；
  - 重置时间片计数；
  - 返回 1 让 `usertrap()` 去调用 `yield()`。

> 所以，哪怕一个优先级最高的进程一直算一直算，每过一个时间片也必须让出 CPU，别人也能获得机会。

---

## 7. `yield()` / `sleep()` / `wakeup()` 在优先级模式下的行为

这几个函数是“进程自己让出 CPU”或“从睡眠中醒来”的入口，它们在优先级调度模式下的行为也要协调好。

### 7.1 `yield()`：主动谦让 CPU

在 `kernel/proc.c`：

```c
void yield(void) {
  struct proc *p = myproc();
  acquire(&p->lock);

  if (sched_policy == SCHED_MLQ) {
    ... // MLQ 下会调整队列层级
  }

  p->state = RUNNABLE;
  rq_enqueue(p);   // 放回就绪队列
  sched();         // 切回调度器
  release(&p->lock);
}
```

在优先级调度模式里：

- `rq_enqueue()` 不使用多级队列，只是把进程标记为 RUNNABLE，并设置统一时间片。
- 进程优先级 `priority` 自己不会因为 `yield()` 而改变。

### 7.2 `sleep()` / `wakeup()`：等待事件 & 被唤醒

标准用法比如 `sys_sleep()`、`wait()` 等都会调用 `sleep(chan, &lock)`。

在优先级模式下：

- `sleep()`：
  - 把进程状态改为 `SLEEPING`，设置 `p->chan`，然后 `sched()`。
- `wakeup(chan)`：
  - 遍历所有进程：
    - 找到 `state == SLEEPING` 且 `p->chan == chan` 的；
    - 调用 `on_wakeup(p)` 把它设为 `RUNNABLE` 并放回就绪队列。

在 `on_wakeup()` 的优先级分支里：

```c
} else {
  // PRIORITY 策略：不使用 MLFQ 层级
  p->qlevel = -1;
  p->rq_next = p->rq_prev = 0;
  rq_enqueue(p);
  p->cur_qslice_left = FIXED_QSLICE_PRIORITY;
  ...
  return 0;  // 默认不强制抢占当前进程
}
```

也就是说：

- 被唤醒的进程只是变成 RUNNABLE，重新参与下一轮“优先级比拼”。
- 当前正在跑的进程不会立刻被抢占（除非时钟中断时间片到了）。
- 如果你想改成“唤醒高优先级进程就抢占”，可以在这里加逻辑：
  - 比较 `p->priority` 和当前 `myproc()->priority`，必要时调用 `yield()`。

---

## 8. fork 时优先级的继承与抢占

在 `fork()` 的底部，代码做了一个小优化：

```c
// 优先级调度：如果子进程优先级更高，触发让出
if (np->priority < p->priority) {
  yield();  // 立刻让出 CPU
}
```

含义：

- 新建出来的子进程 `np` 的优先级如果比父进程 `p` 更高：
  - 父进程主动 `yield()`。
- 这样可以让“高优先级子进程”更快地获得 CPU，符合直觉。

---

## 9. 小结：你可以怎么验证这个优先级调度？

可以重点用 `user/mytest2.c` 里的测试来感受效果：

1. 先创建多个子进程，让它们执行一些 CPU 密集任务。
2. 使用不同的 `setpriority(pid, value)` 设置优先级：
   - 数值小的（比如 5）应该比数值大的（比如 25）更频繁得到 CPU。
3. 观察输出的打印顺序、完成时间差异。

再配合：

- 修改时间片长度 `FIXED_QSLICE_PRIORITY`（比如改成 3 或 20），再次观察：
  - 时间片越小，进程切换越频繁。
  - 时间片越大，切换开销小但响应也更慢。

结合本文“先看体系，再对照源码”的方式，多看几遍就会对“优先级调度”的实现有一个完整的心智模型。

如果你已经理解了本篇，再去看《多级队列调度实现讲解》那篇，就会更轻松。

